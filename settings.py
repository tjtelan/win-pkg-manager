import os
import wpm_db
import db_wrapper
#import time

class settings:

    def __init__:
        DEFAULT = {
            'PACKAGE_DIR'   : os.curdir + "/packages",
            'DOWNLOAD_DIR'  : os.curdir + "/downloads"
        }
